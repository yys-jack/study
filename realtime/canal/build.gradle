configurations.configureEach {
    exclude group: 'org.slf4j', module: 'slf4j-log4j12'
    // 可选：通常有了 logback 就不再需要 log4j 本体
    exclude group: 'log4j', module: 'log4j'
}

dependencies {
    implementation 'com.alibaba.otter:canal.client:1.1.0'

}


